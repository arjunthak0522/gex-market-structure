import type {OptionContract} from './gex';
const BASE='https://api.schwabapi.com/marketdata/v1';
export async function getSchwabChain(symbol:'SPY'|'QQQ',accessToken:string){const url=new URL(`${BASE}/chains`);url.searchParams.set('symbol',symbol);url.searchParams.set('contractType','ALL');url.searchParams.set('includeUnderlyingQuote','true');url.searchParams.set('strategy','SINGLE');const r=await fetch(url,{headers:{Authorization:`Bearer ${accessToken}`},cache:'no-store'});if(!r.ok)throw new Error(`Schwab chain request failed: ${r.status}`);return r.json()}
export function normalizeSchwabChain(payload:any):{spot:number;contracts:OptionContract[]}{const spot=Number(payload?.underlyingPrice??payload?.underlying?.last??payload?.underlying?.mark??0);const contracts:OptionContract[]=[];const ingest=(map:any,putCall:'CALL'|'PUT')=>{for(const exp of Object.values(map??{}) as any[]){for(const strikeGroup of Object.values(exp??{}) as any[]){for(const c of strikeGroup??[]){const strike=Number(c.strikePrice??c.strike);const gamma=Number(c.gamma);const openInterest=Number(c.openInterest);if(Number.isFinite(strike)&&Number.isFinite(gamma)&&Number.isFinite(openInterest))contracts.push({strike,gamma,openInterest,putCall,expirationDate:c.expirationDate,daysToExpiration:c.daysToExpiration})}}}};ingest(payload?.callExpDateMap,'CALL');ingest(payload?.putExpDateMap,'PUT');return{spot,contracts}}
